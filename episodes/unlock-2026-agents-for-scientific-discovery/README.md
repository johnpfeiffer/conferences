# UNLOCK 2026: "Agents for Scientific Discovery" — Andrew White (2026-04-22)

Artifact series generated by running this repo's `get-transcript` + `fix-transcript`
skills against the session's YouTube video. The original transcript in
`app/src/data/transcripts/` is untouched; this folder is a standalone re-derivation.

- Video: https://www.youtube.com/watch?v=BzRvHeBC9ZY (UNLOCK 2026 playlist, index 3)
- Speaker: Andrew White, Co-founder & CTO, Edison Scientific (Kosmos)

## Files

| File | Role |
|---|---|
| `youtube_raw.txt` | Immutable raw. Byte-identical copy (md5 `5c5fe9b2…`) of `app/src/data/transcripts/unlock-2026-agents-for-scientific-discovery.txt` — see fetch note below |
| `proposed-transcript-fixes.json` | 20 fix rules: 11 inherited unchanged from the repo's owner-authored fixes + 9 found by this pass's suspicious-token discovery |
| `fixed_transcript.txt` | Raw + all 20 rules applied with `transcriptFixes.ts`'s exact algorithm: **39 corrections** (the site's own fixed view applies 27; this pass adds 12) |
| `glossary.json` | 25 episode-scoped terms with per-entry `sources` provenance; reconciled against the repo's 250-term conference glossary and knowledge graph |
| `COMPARISON.md` | Method notes: fetch blockage, delta vs the site fix, discovery-pass findings |

## Fetch note (2026-08-23)

Fresh caption download was not possible: YouTube bot-gated every yt-dlp player
client and `youtube-transcript-api` from this environment's IP (the
`player_client=android,web_embedded` bypass that worked on 2026-08-22 was no
longer sufficient; no browser cookies available). Per the get-transcript skill's
fallback ladder, the owner-copied YouTube-panel transcript already in the repo —
the same ASR source — served as the immutable raw basis, recorded here verbatim.

## This pass's discoveries (beyond the repo's existing 11 fixes)

- `ChemCro` → **ChemCrow**, `the co-scientist paper` → **Coscientist**,
  `Gabe Gomez` → **Gabe Gomes**, `Shenu Yao` → **Shunyu Yao**,
  `react miracle paper` → **ReAct** (the 2022-2023 agent-paper history passage)
- `Nova` → **Novo** (Nordisk; phrase is split across a timestamp marker in the
  panel format, so the fix targets the in-chunk fragment)
- `bioarchchive` → **bioRxiv** (x2)
- `the providence of all the claims` → **the provenance of all the claims**
- Orthographic: `whack-a-ole` → `whack-a-mole`, `postocs` → `postdocs`,
  `coalesed` → `coalesced`

## Still unresolved (documented, not guessed)

- **"Luzison"** (4:46) — location of the IBM cloud lab the agents operated;
  candidates are IBM Research sites (Zurich/Rüschlikon). Verify against recording.
- **"Antonio's"** (13:26) — first-name-only citation of a recent paper.
- **"Sam"** (~2:59) — Edison co-founding reference; likely Sam Rodriques, unverified.
